{
  "name": "mnemosyne-memory",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test core.test.ts",
    "typecheck:pi": "tsc --noEmit --module esnext --moduleResolution bundler --target es2024 --skipLibCheck pi.ts core.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "typescript": "^5.9.3"
  },
  "pi": {
    "extensions": [
      "./pi.ts"
    ]
  }
}
