{
  "name": "mnemosyne-memory",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit --module esnext --moduleResolution bundler --target es2024 --skipLibCheck config.ts core.ts pi.ts hooks.ts index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "@oh-my-pi/pi-coding-agent": "17.2.15",
    "typescript": "^5.9.3"
  },
  "pi": {
    "extensions": [
      "./pi.ts"
    ]
  }
}
